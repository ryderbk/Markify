import { z } from "zod";

// Markify is a stateless calculator application - no data persistence needed
// This schema file is kept for future extensibility if needed

export const emptySchema = z.object({});
